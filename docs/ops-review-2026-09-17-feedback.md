# 2026-09-17 问题反馈功能发布记录

## 发布范围

- 将本地已验证的问题反馈工作区快照发布到 `https://botcode.work`，生产目录 `/www/oj`、PM2 进程 `oj`。
- 包含学生/老师提交与本人反馈查询、管理员筛选/追加回复/状态管理、私有截图读取及 `0019_problem_feedback`。
- 部署阶段尚未推送 GitHub；构建源为 `589e687` 基础上的本次反馈功能工作区快照，而非预先创建的 Git 提交。GitHub 同步在部署后单独执行，不改变下述发布包与生产数据。
- 本地此前验证：635 项单元测试、9 项 E2E、TypeScript、Lint 与隔离目录生产构建通过。本次另完成 Linux 生产构建和生产只读冒烟。

## 构建与备份

- 遵循低内存部署流程：本机 `node:22-bookworm-slim` Docker 容器安装 OpenSSL 3、执行 `npm ci --include=dev`、Prisma generate 和 Next build；服务器未运行 npm ci 或构建。
- 依赖声明变化，上传同次 Linux 构建的完整根依赖，不复用旧根 node_modules；服务器 Node 20.20.2 下 sharp 编解码检查成功。
- 发布包：`/www/oj-feedback-20260917.tgz`，295523717 字节。
- SHA-256：`6dd80ad33e2aa3464a6a3e8aedf4c8401d14b4f484e063b125b057caf02c7ff7`，上传前后吻合。
- 审计 42594 个归档条目，未发现环境文件、数据库、备份、嵌套压缩包或私钥文件；Prisma 引擎为 `debian-openssl-3.0.x`。
- 在线预备份：`/www/backups/prod-20260917-124550-before-feedback.db`。
- 停服切换备份：`/www/backups/prod-20260917-feedback-cutover.db`，8638464 字节；SQLite backup API 生成、完整性检查通过、权限 0600。
- 旧版本：`/www/oj-old-20260917-feedback`，保留切换时原库和环境。未删除历史备份或其它站点文件。

## 切换与验证

- 切换前及停止服务后检查有效进行中考试，均为 0；没有修改考试记录或触发学生登录结算。
- 停止 PM2 后备份最新数据库并复制到新目录，切换目录后才执行 db:deploy；0019 成功，19 个迁移全部完成。
- PM2 通过原有 npm start 入口重启。监听为 `127.0.0.1:3000`；根目录 0755、生产 .env 0600。
- 服务器直连、公网域名及本机外部访问的健康检查通过。登录页、CSS/JS、AC 图片返回 200；CSS gzip、immutable、HSTS 与 nosniff 正常。
- 三种角色反馈页面与对应列表 API 返回 200；未登录 API 返回 401，学生/老师调用管理员反馈 API 返回 403，不存在详情返回 404，跨站写请求返回 403，API 为 no-store。
- 角色冒烟仅做服务端临时签名会话的只读请求，不打印令牌，不更改账号、密码或会话版本。生产库未写入测试反馈；提交、上传、回复和状态切换的完整流程由本地隔离测试验证。
- 登录 API 空请求正确返回 400，不尝试真实账号密码。
- Docker Judge 通过实际应用判题函数编译运行输出 42 的程序，结果 Accepted；未创建 Submission。
- SQLite integrity_check 为 ok，foreign_key_check 无异常。切换前后数量一致：用户 50、题目 1590、提交 2216、考试 2、考试记录 2、测试结果 5585；三张反馈表初始化为空。
- 站点图标由动态配置负责，新旧 public 均无 favicon.ico；未将该历史 404 当作发布故障。
- 部署阶段未执行 db:init、seed、生产依赖升级、Nginx 覆盖或全局 Docker 清理。

## 回滚注意

旧版本和切换备份均已保留。回滚前先停止 OJ 并备份当前生产库，按 `docs/deploy.md` 执行；上线后如已有新反馈或提交，不能直接恢复切换前数据库而丢弃新增数据。本次迁移为新增表，不需要为回滚代码直接删除反馈表。
