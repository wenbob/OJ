# 2026-08-05 选择判断答案区高度与滚动体验修正记录

## 发布范围

- 学生、老师、管理员共用的选择判断答案编辑器统一使用 `clamp(360px, 44dvh, 520px)`，相较原 AI 紧凑布局约扩大一倍。
- 学生日常/专项、学生考试、后台题目练习和后台考试练习全部复用同一高度规则。
- 选择判断 AI 页面改为“解析在上、答案输入在下”的自然文档流；答案区外层不再设置横向或纵向内部滚动，统一使用主页面滚动。
- 长 AI 解析仍保留解析面板自己的滚动，不改变编程题编辑器、判题、权限、数据库或 API。

## 本地验证

- `npx prisma validate`：通过。
- `npm run test`：90 个测试文件、474 项测试通过。
- `npx tsc --noEmit`：通过。
- `npm run lint`：通过。
- 本地浏览器验证老师校题页：答案区 `overflow-x/y` 均为 `visible`，没有横向或纵向溢出，编辑器高度为 360px，控制台无错误。
- 受限 Docker 构建：2 CPU、2GB 容器、单构建 worker、Node 最大堆 1024MB；Next.js Linux standalone 构建通过。
- 临时 SQLite 完整执行 15 项迁移；发布包仅包含 OpenSSL 3 Prisma 引擎。

## 发布包

- 文件大小：54,921,251 字节。
- SHA-256：`725a517b054d492e01dbbec9aa936391c15e59a78312356ab859101789b8cce6`。
- 归档审计通过：不含 `.env`、数据库、根 `node_modules`、`.next/cache` 或嵌套压缩包。
- 包内运行验证通过：健康接口、登录页、真实 CSS 和 `/ac-success.png` 均返回 200。

## 生产切换

- 生产数据库备份：`/www/backups/prod-before-20260805-191859-answer-editor.db`，`PRAGMA quick_check` 为 `ok`。
- 保留旧版本：`/www/oj-old-20260805-192111-answer-editor`。
- 生产已有 15 项迁移，本次没有待执行迁移。
- 切换前后业务计数保持 `29|1350|1526|2335|1`（用户、题目、提交、测试点、专项练习）。

## 上线验证

- PM2 `oj` 在线，监听 `127.0.0.1:3000`。
- 公网健康接口、登录页、CSS 和 AC 图片均正常。
- 数据库完整性、权限拒绝和 Docker Judge C++17 编译运行验证通过。
- 最近 Nginx 访问记录中的 504 数量为 0，AC 图片没有重新进入 `/_next/image` 优化器。
- 上传的临时发布包已从服务器删除；数据库备份和旧版本目录保留用于回滚。
