# 2026-07-12 天梯积分差展示上线记录

## 变更内容

学生天梯榜“我的战绩”同时展示两个不同口径：

- 距离前一名：上一排名学生积分减去当前学生积分。
- 距离第一名：榜首学生积分减去当前学生积分。

如果当前学生与对比对象积分相同，页面显示“积分相同”，不显示“还差 0 分”。本次只增加展示数据，没有修改积分、唯一 AC、段位或排名排序规则。

## 验证与发布

- `npm run test`：21 个测试文件、118 项测试通过。
- `npx tsc --noEmit` 和 `npm run lint` 通过。
- 本地 Docker 生成并审计 Linux standalone 发布包，服务器未执行 `npm ci` 或 Next.js 构建。
- 功能提交：`803e268 Show gap to leaderboard first place`。

上线前数据库备份：

```text
/www/backups/prod-20260712-094429-before-first-gap.db
```

保留的回滚目录：

```text
/www/oj-old-20260712-094429-first-gap
```

线上确认 PM2 和数据库健康，编译后的服务器代码包含新文案，公网登录页及抽查的 10 个静态资源正常，C++ Judge 编译运行冒烟通过。

## 清理

新回滚点确认后删除被取代的 `/www/oj-old-20260711-101855-arena-ui` 和服务器发布压缩包；未触碰其它网站、股票系统或 Docker 全局资源。
