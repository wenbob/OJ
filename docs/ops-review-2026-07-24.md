# 2026-07-24 管理员历史通过状态与快捷入口上线记录

## 上线范围

- 管理员题目练习列表、题目详情和模拟考试练习题单统一显示当前管理员账号的历史“已通过”状态。
- 状态按该管理员全部历史 `Accepted` 实时计算，日常与考试来源均计入；后续提交失败不会取消。
- “已通过”旁提供快捷入口：编程题显示“查看通过代码”，选择判断题显示“查看通过答案”。
- 快捷入口打开该管理员最近一次 Accepted 的提交详情；时间相同时以较大的提交 ID 为准。
- 题目练习列表的已通过行使用淡绿色背景，操作文案改为“再次练习”；题目详情提交成功后刷新服务端状态。
- 原有“最近一次提交”和日常提交数量口径保持不变，避免把历史通过状态与最新尝试混淆。

本次没有新增数据库字段、迁移、管理 API 或第三方依赖。

## 本地验证与构建

- `npm run test`：64 个测试文件、300 项测试通过。
- `npx tsc --noEmit` 和 `npm run lint` 通过。
- 使用临时 SQLite 数据库完成真实服务端渲染集成验证，确认三个管理员校题页面都按当前账号展示通过状态及正确快捷链接。
- 在受限本地 Linux Docker 环境执行 `npm ci --include=dev`、Prisma generate 和 Next standalone 构建；服务器未执行 `npm ci` 或 Next.js 构建。
- 发布包大小为 55,948,784 字节，SHA-256 为 `e169dbf243d2246aa9bd2d0a3408c021c6c51c72eb18e096a6e7e560dda8ad20`。
- 归档共 4,570 个条目，不含 `.env`、数据库、根 `node_modules`、`.next/cache` 或嵌套压缩包；Prisma 仅包含 OpenSSL 3 引擎，并已包含 standalone 静态资源和 `public`。

## 生产发布与回滚点

- 生产数据库备份：`/www/backups/prod-20260724-103553-before-admin-accepted-status.db`。
- 旧版本回滚目录：`/www/oj-old-20260724-103553`。
- 依赖锁文件未变化，服务器通过硬链接复用原有根 `node_modules`。
- 服务器未执行 `npm ci`、Next.js 构建、`seed` 或数据库初始化。
- 切换后执行迁移检查：10 个迁移均已应用，没有待执行项。
- 备份库与上线库 SQLite 完整性检查均为 `ok`。

## 线上验收

- PM2 `oj` 在线且无不稳定重启，工作目录为 `/www/oj`。
- 本机和公网 `/api/health` 均返回数据库正常；服务只监听 `127.0.0.1:3000`。
- 登录页、实际 CSS/JS 静态资源和公共设置接口均返回 200；空登录请求返回受控的 400。
- 生产数据计数为用户 27、题目 1,330、提交 1,450、测试点 2,295。
- 使用真实管理员会话验证题目练习列表和详情均显示“已通过”“再次练习”“查看通过代码”，快捷提交详情返回 200。
- 通过正式 `/api/problems/[id]/run` 路径完成 Docker Judge 冒烟，输出为 `OJ_SMOKE`；试运行前后 `Submission` 均为 1,450 条。
- PM2 历史日志中存在目录切换后旧页面触发的 Server Action 失效记录；刷新新页面后复查日志大小和时间均未继续增长，不属于当前持续故障。

## 洁癖收尾

- 生产验收前后均确认健康检查正常，随后删除服务器临时发布包 `/www/oj-release.tgz`。
- 删除本地 `.release-work` 发布临时目录。
- 保留本次数据库备份和旧版本回滚目录；未清理其它网站、股票系统、Docker 全局缓存、镜像或 volume。
- README、管理员手册和 `AGENTS.md` 已与当前行为同步；学生手册无需增加管理员专用入口。
